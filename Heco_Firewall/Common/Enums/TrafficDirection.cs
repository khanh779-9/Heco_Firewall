namespace Heco.Common.Enums;

/// <summary>
///   Specifies the direction of network traffic relative to the local machine.
/// </summary>
public enum TrafficDirection
{
    /// <summary>Outbound network traffic (sent from local machine).</summary>
    Outbound,
    /// <summary>Inbound network traffic (received by local machine).</summary>
    Inbound
}
